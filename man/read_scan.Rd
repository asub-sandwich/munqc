% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_scan.R
\name{read_scan}
\alias{read_scan}
\title{Read colour scans from CSV or Excel}
\usage{
read_scan(
  path,
  sensor = SENSORS,
  illuminant = ILLUMINANTS,
  observer = OBSERVERS,
  chip = NULL,
  L = NULL,
  a = NULL,
  b = NULL,
  hue = NULL,
  value = NULL,
  chroma = NULL,
  book_id = NULL,
  finish = NULL,
  sheet = NULL,
  ...
)
}
\arguments{
\item{path}{Path to a \code{.csv}, \code{.tsv}, \code{.txt}, \code{.xlsx} or \code{.xls} file. May be
a vector of paths, in which case each file becomes one book.}

\item{sensor, illuminant, observer}{Passed to \code{\link[=scan_collection]{scan_collection()}}.}

\item{chip, L, a, b}{Names of the corresponding columns. \code{NULL} to autodetect.}

\item{hue, value, chroma}{Names of separate Munsell component columns, used
when there is no single condensed \code{chip} column.}

\item{book_id}{Either the name of a column holding book identifiers, or a
literal label to apply to every row. When \code{NULL}, the file name is used.}

\item{finish}{Name of a column giving each chip's surface finish. Usually
unnecessary, since finishes are looked up from \link{chipfinish}.}

\item{sheet}{Sheet name or number, for Excel files only.}

\item{...}{Passed on to \code{\link[utils:read.csv]{utils::read.csv()}} or \code{\link[readxl:read_excel]{readxl::read_excel()}}.}
}
\value{
A \code{ScanCollection}.
}
\description{
Reads one or more scan files and returns a \code{\link[=scan_collection]{scan_collection()}} ready for
\code{\link[=compute_error]{compute_error()}}.
}
\details{
Column names are detected automatically where possible: \code{chip}, \code{L}, \code{a},
\code{b} and common variants (\verb{L*}, \code{lightness}, \code{munsell}, \code{book}, ...) are
recognised case-insensitively and ignoring punctuation. Anything the
detector cannot find, or finds twice, must be named explicitly.

Chips may be given either as condensed notation in a single column
(\code{"10YR 8/1"}) or as separate \code{hue}, \code{value} and \code{chroma} columns, which are
joined for you.
}
\examples{
f <- tempfile(fileext = ".csv")
write.csv(
  data.frame(
    Munsell = c("10YR 8/1", "10YR 8/2"),
    `L*` = c(81.3, 80.9), `a*` = c(1.3, 3.1), `b*` = c(6.6, 13.5),
    check.names = FALSE
  ),
  f, row.names = FALSE
)

read_scan(f, sensor = "veykolor")

# Explicit mapping when autodetection can't help
read_scan(f, sensor = "veykolor", chip = "Munsell", L = "L*")

unlink(f)

}
