% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tolerance.R
\name{tolerance_window}
\alias{tolerance_window}
\title{Tolerance window around reference chips}
\usage{
tolerance_window(
  chips = NULL,
  de = NULL,
  thresholds = munqc_thresholds(),
  sensor = SENSORS,
  n = 72L,
  iterations = 40L,
  max_radius = 64
)
}
\arguments{
\item{chips}{a \code{data.frame} with columns \code{chip}, \code{L}, \code{a}, \code{b}. Defaults
to the reference chips for \code{sensor}.}

\item{de}{Target distance. Defaults to the matte cut of \code{thresholds}.}

\item{thresholds}{A \code{\link[=munqc_thresholds]{munqc_thresholds()}} object, used only when de is
\code{NULL}.}

\item{sensor}{Which reference to use when \code{chips} is not supplied.}

\item{n}{Number of points traced around each window. 72 is enough for most
plots.}

\item{iterations}{Bisection steps. 40 resolves the window radius for plotting
purposes.}

\item{max_radius}{Largest radius searched, in Lab units.}
}
\value{
a \code{data.frame} with one row per traced point: \code{chip}, \code{L}, \code{C}, \code{a},
\code{b}, and the page-space coordinates \code{x} and \code{y}.
}
\description{
Creates a window of colors sitting exactly dE away from each
reference chip in CIE dE2000. Drawn on a page-space plot, these help
visualize the 'window' in which measured chips may fall into and still be
recognizable as the original color.
}
\examples{
w <- tolerance_window(sensor = "veykolor", de = 3, n = 24)
head(w)

}
