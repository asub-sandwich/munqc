% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compute_error}
\alias{compute_error}
\title{Score a scan collection against the reference books}
\usage{
compute_error(x, thresholds = munq_thresholds())
}
\arguments{
\item{x}{A \code{\link[=scan_collection]{scan_collection()}}.}

\item{thresholds}{A \code{\link[=munq_thresholds]{munq_thresholds()}} object. Defaults to failing at
\eqn{\Delta E_{00} \ge 3}.}
}
\value{
\code{x} with \verb{$results} and \verb{$thresholds} populated. \verb{$results} is a
\code{data.frame} with one row per scanned chip and columns \code{book_id}, \code{chip},
\code{hue}, the scanned and reference \code{L}/\code{a}/\code{b}, \code{delta_e_2000}, \code{grade}, and
\code{fail}.
}
\description{
Computes CIEDE2000 (\eqn{\Delta E_{00}}) between each scanned chip and the
corresponding chip in \link{colordata}, then grades it against \code{thresholds}.
}
\examples{
df <- data.frame(
  book_id = "my-book",
  chip = c("10YR 8/1", "10YR 8/2"),
  L = c(81.3, 80.9), a = c(1.3, 3.1), b = c(6.6, 13.5)
)
sc <- scan_collection(df, sensor = "veykolor")
compute_error(sc)

# Stricter grading
compute_error(sc, munq_thresholds(fail_at = 1))

}
\seealso{
\code{\link[=qc_summary]{qc_summary()}} for book- and page-level rollups.
}
