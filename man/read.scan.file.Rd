% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan_data.R
\name{read.scan.file}
\alias{read.scan.file}
\title{Read a scan file (CSV or TXT) into a munqc scan collection tibble.}
\usage{
read.scan.file(path, sensor, delim = NULL, ...)
}
\arguments{
\item{path}{Path to a \code{.csv} or \code{.txt} file.}

\item{sensor}{Character string naming the sensor. Required.}

\item{delim}{Delimiter character. \code{NULL} (default) auto-detects from the
file extension: \verb{,} for \code{.csv}, \verb{\\t} for \code{.txt}.}

\item{...}{Additional arguments passed to \code{\link[utils:read.table]{utils::read.table()}}.}
}
\value{
A \code{munq.scan} object (see \code{\link[=munq.scan]{munq.scan()}}).
}
\description{
Reads a delimited file and auto-detects which chip identification scheme is
present. Recognised column-name patterns are case-insensitive and accept
common variants (e.g. \code{"L*"}, \code{"Lstar"}, \code{"L_star"} all map to \code{L}).
}
\details{
\strong{Supported layouts}\tabular{ll}{
   Layout \tab Required columns \cr
   chip-only \tab \code{chip}, \code{L}, \code{a}, \code{b} \cr
   full Munsell \tab \code{h}, \code{v}, \code{c}, \code{L}, \code{a}, \code{b} \cr
}
}
\examples{
\dontrun{
scan <- read.scan.file("my_scan.csv", sensor = "nix")
scan <- read.scan.file("my_scan.txt", sensor = "konicaminolta", delim = ",")
}

}
